# Automated Pentest Tool — web-focused runtime dependencies
#
# External security binaries are intentionally not installed by pip. Install
# and version them separately using the tool manifest in
# WEB_PENTEST_REQUIREMENTS.md.

# Existing async scanner and web crawler
aiohttp>=3.9,<4
aiodns>=3.2,<4
aiofiles>=23.2,<25
beautifulsoup4>=4.12,<5
certifi>=2024.2.2,<2026
# lxml 5.x may fall back to an incompatible source build on Python 3.14.
# lxml 6.x provides Python 3.14-compatible distributions.
lxml>=6.0,<7

# Configuration, CLI, logging, and API
PyYAML>=6.0,<7
colorama>=0.4.6,<1
click>=8.1,<9
loguru>=0.7,<1
Flask>=3.0,<4

# Web platform upgrade: background jobs, API, and persistence
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3
SQLAlchemy>=2.0,<3
alembic>=1.13,<2
redis>=5.0,<6
celery>=5.4,<6
python-multipart>=0.0.9,<1

# Reports and machine-readable output
Jinja2>=3.1,<4
Markdown>=3.7,<4
orjson>=3.10,<4
reportlab>=4.0,<5

# DNS/TLS metadata and risk scoring
dnspython>=2.6,<3
cryptography>=43,<46
cvss>=3.1,<4

# Development and verification
pytest>=8,<9
pytest-asyncio>=0.23,<1
pytest-cov>=5,<7
ruff>=0.6,<1
mypy>=1.11,<2
